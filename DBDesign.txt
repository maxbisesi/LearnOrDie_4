
Mission statement: 

The purpose of the FlashCardShark database is to keep track of support the LearnOrDie application.

Mission objectives:

Keep track of all site users.

Store FlashCards created by users.

Store attributes and items related to the users.

Keep track of users' sessions, card they see, dates, ratings etc.

Store Notes the users make.

Store sets of cards users create.

Subject List


Avatars done
    - avatar_id
    - style varchar
    - birthstar varchar
    - fk_user_id
    - fk_primary_weapon_id
    - fk_secondary_weapon_id
    

CardSets
    -setid
    -setname
    -description

CardSetCards
    - fk_cardset_id
    - fk_card_id

FlashCards
    -cardid
    -card
    -answer
    -category

FlashCardUsers
    -fk_card_id
    -fk_user_id
    -correct
    -incorrect
    -fk_note_id

FlashUsers done 
    -user_id
    -username
    -password
    -email
    -points

Notes
    -noteid
    -note
    -fk_user_id CPK
    -fk_card_id CPK

Weapons done
    -weaponid
    -name
    -Primary boolean
    -damage

UserSessions
    -session_id
    -cards_seen
    -correct
    -incorrect
    -date
    -fk_user_id